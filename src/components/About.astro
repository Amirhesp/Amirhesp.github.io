---
import { profile } from '../data/profile';
import Section from './Section.astro';
---

<Section id="about" title="About">
  <div class="space-y-4 text-base leading-relaxed sm:text-lg">
    {profile.bio.map((paragraph) => <p>{paragraph}</p>)}
  </div>
</Section>
